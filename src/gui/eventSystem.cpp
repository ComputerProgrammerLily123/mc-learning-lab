#include "eventSystem.h"
namespace GUI
{
    void EventSystem::Subscribe(Event event, std::function<void()> eventHandler)
    {
    }
    void EventSystem::UnSubscribe(Event event, unsigned long index)
    {
    }
}